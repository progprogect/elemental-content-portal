# Инструкция по установке расширения

## Шаг 1: Сборка расширения

Перед установкой расширения необходимо скомпилировать TypeScript код:

```bash
cd extension
npm install
npm run build
```

После сборки в директории `extension/src/` появятся скомпилированные `.js` файлы.

## Шаг 2: Установка в Chrome/Edge

1. Откройте браузер Chrome или Edge
2. Перейдите в настройки расширений:
   - **Chrome**: `chrome://extensions/`
   - **Edge**: `edge://extensions/`
3. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
4. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
5. Выберите папку `extension/` (корневую папку расширения, где находится `manifest.json`)

## Шаг 3: Проверка установки

После установки вы должны увидеть расширение "Elemental Content Creation Portal" в списке расширений.

## Шаг 4: Использование

1. Откройте портал контента (http://localhost:5173 или ваш production URL)
2. Откройте задачу и выберите публикацию
3. Нажмите кнопку **"🎬 Generate Content"**
4. Расширение автоматически:
   - Откроет Haygen в новой вкладке
   - Заполнит промпт
   - Загрузит все ассеты
   - Будет мониторить результаты генерации

## Разработка

Для разработки с автоматической пересборкой при изменениях:

```bash
cd extension
npm run watch
```

После изменений в коде:
1. Дождитесь завершения компиляции
2. Перезагрузите расширение в браузере (кнопка обновления на карточке расширения)

## Troubleshooting

### Расширение не работает
- Убедитесь, что расширение собрано (`npm run build`)
- Проверьте консоль расширения: `chrome://extensions/` → "Просмотр сервис-воркера" (View service worker)
- Проверьте консоль страницы Haygen (F12)

### Промпт не заполняется автоматически
- Проверьте, что вы на странице `*.haygen.com`
- Убедитесь, что расширение активно (иконка не серая)
- Попробуйте перезагрузить страницу Haygen

### Ассеты не загружаются
- Проверьте, что URL ассетов доступны (нет CORS ошибок)
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что файлы имеют правильный формат

### Результаты не сохраняются
- Проверьте, что портал открыт в другой вкладке
- Убедитесь, что расширение имеет необходимые permissions
- Проверьте консоль расширения на наличие ошибок

